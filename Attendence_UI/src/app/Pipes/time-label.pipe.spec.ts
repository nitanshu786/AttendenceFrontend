import { TimeLabelPipe } from './time-label.pipe';

describe('TimeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
